# AGENTS.md

## Project
FastAPI demo with pytest.

## How to run tests
Run `./SETUP.sh` to install dependencies and run tests.

## Conventions
- Code lives in `app/`
- Tests live in `tests/`
- Add new endpoints with tests first.



